% Message flow MessageFlow_2 should be visible only as a synchronized communication action.
% The message must be observable as a synchronized communication, not as raw send/receive actions.
<true*>(<exists oid: OrderId. c_send_manifest(oid)>true) &&
[true* . (exists oid: OrderId. s_send_manifest(oid))]false &&
[true* . (exists oid: OrderId. r_send_manifest(oid))]false