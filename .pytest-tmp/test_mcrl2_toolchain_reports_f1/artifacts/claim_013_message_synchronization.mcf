% Message flow MessageFlow_3 must synchronize Task_SAG_Send2 with ReceiveTask_2.
% The message must be observable as a synchronized communication, not as raw send/receive actions.
<true*>(<exists oid: OrderId. c_send_eir(oid)>true) &&
[true* . (exists oid: OrderId. s_send_eir(oid))]false &&
[true* . (exists oid: OrderId. r_send_eir(oid))]false