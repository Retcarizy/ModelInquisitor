% Message flow MessageFlow_1 must synchronize Task_FFW_Send with ReceiveTask_3.
% The message must be observable as a synchronized communication, not as raw send/receive actions.
<true*>(<exists oid: OrderId. c_send_request(oid)>true) &&
[true* . (exists oid: OrderId. s_send_request(oid))]false &&
[true* . (exists oid: OrderId. r_send_request(oid))]false